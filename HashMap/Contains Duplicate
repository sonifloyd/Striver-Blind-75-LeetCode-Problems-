import java.util.*;

public class Main {

    public static boolean containsDuplicate(int[] nums) {
        HashSet<Integer> set = new HashSet<>();

        for (int num : nums) {
            if (set.contains(num)) {
                return true; // duplicate mil gaya
            }
            set.add(num);
        }

        return false;
    }

    public static void main(String[] args) {
        int[] nums = {1, 2, 3, 1};

        System.out.println(containsDuplicate(nums)); // true 
    }
}
---------------------------
HashMap<Integer, Integer> map = new HashMap<>();

for (int num : nums) {
    if (map.containsKey(num)) {
        return true;
    }
    map.put(num, 1);
}
-----Find Duplicate -------------
import java.util.*;

public class Main {

    public static Integer findDuplicate(int[] nums) {
        HashSet<Integer> set = new HashSet<>();

        for (int num : nums) {
            if (set.contains(num)) {
                return num; // duplicate number return
            }
            set.add(num);
        }

        return null; // agar duplicate nahi mila
    }

    public static void main(String[] args) {
        int[] nums = {1, 2, 3, 1};

        System.out.println(findDuplicate(nums));
    }
}
