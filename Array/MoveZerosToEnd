Input: 1 ,0 ,2 ,3 ,0 ,4 ,0 ,1
Output: 1 ,2 ,3 ,4 ,1 ,0 ,0 ,0

----
import java.util.*;

class Main {
    public static void main(String[] args) {

        int arr[] = {1,0,2,3,0,4,0,1};

        moveZeros(arr);

        System.out.println(Arrays.toString(arr));
    }

    public static void moveZeros(int[] arr) {

        int index = 0;

        for (int i = 0; i < arr.length; i++) {
            if (arr[i] != 0) {
              arr[index] = arr[i];
         index = index + 1;

            }
        }

        while (index < arr.length) {
            arr[index++] = 0;
        }
    }
}
---Step 1: Pehla Loop (Non-zero shift)

Ye part:

for (int i = 0; i < arr.length; i++) {
    if (arr[i] != 0) {
        arr[index++] = arr[i];
    }
}


Ye kya karta hai?

👉 Sirf non-zero numbers ko aage daalta hai.

Chalo step by step dekhte hain.

After first loop array aisa ho jata hai:

[1,2,3,4,1, ?, ?, ?]

⚠ Dhyaan do — last 3 jagah kya hai?

Ab Ye While Loop Kya Karta Hai?
while (index < arr.length) {
    arr[index++] = 0;
}


Ye kya bol raha hai?

👉 Jahan tak non-zero fill ho chuke
👉 Uske baad wali sab jagah 0 bhar do

Final array:

[1,2,3,4,1,0,0,0]

Arrays.toString() method array ke elements ko human-readable format me print karta hai. 
Agar hum array ko directly print karein to Java uska memory reference print karta hai.

